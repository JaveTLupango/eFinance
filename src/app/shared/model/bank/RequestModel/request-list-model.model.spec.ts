import { RequestListModel } from './request-list-model.model';

describe('RequestListModel', () => {
  it('should create an instance', () => {
    expect(new RequestListModel()).toBeTruthy();
  });
});
