import { ContributionBreakDown } from './contribution-break-down';

describe('ContributionBreakDown', () => {
  it('should create an instance', () => {
    expect(new ContributionBreakDown()).toBeTruthy();
  });
});
