import { AccountLoansRequest } from './account-loans-request.model';

describe('AccountLoansRequest', () => {
  it('should create an instance', () => {
    expect(new AccountLoansRequest()).toBeTruthy();
  });
});
